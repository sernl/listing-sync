[package]
name    = "tam-server"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
axum = "0.8"
tam-api = { path = "../tam-api" }
tokio = { version = "1", features = ["rt-multi-thread", "net", "macros", "signal"] }

[lints]
workspace = true
