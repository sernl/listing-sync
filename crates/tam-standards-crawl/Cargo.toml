[package]
name    = "tam-standards-crawl"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
serde_json = "1"
tam-marketplace = { path = "../tam-marketplace" }
tam-marketplace-tpt = { path = "../tam-marketplace-tpt" }
tam-standards = { path = "../tam-standards" }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time"] }

[lints]
workspace = true
