[package]
name    = "tam-domain"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
tam-marketplace = { path = "../tam-marketplace" }
tam-types       = { path = "../tam-types" }

[dev-dependencies]
proptest = "1"

[lints]
workspace = true
