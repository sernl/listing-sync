[package]
name    = "tam-domain"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
# The ledger vocabulary the device exchanges is defined once and derived
# here, so the wire shapes and the domain types cannot drift apart.
serde = { version = "1", features = ["derive"] }
# The plan vocabulary the entitlement token carries, defined once where the
# capability table is.
tam-limits      = { path = "../tam-limits" }
tam-marketplace = { path = "../tam-marketplace" }
tam-types       = { path = "../tam-types" }

[dev-dependencies]
proptest = "1"

[lints]
workspace = true
