[package]
name    = "tam-session-broker"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "net", "macros", "signal"] }

[lints]
workspace = true
