/**
 * What teachers have said, for the band under the pricing section.
 *
 * Empty, and the band renders nothing while it is: the founder's decision of
 * 2026-09-11 is that no invented quote ships. The heading and the card shape
 * live in `components/Testimonials.astro`, so adding an entry here is the whole
 * of publishing the band.
 *
 * Each entry: `quote`, the teacher's own words; `who`, how they want to be
 * named; `stars`, out of five, or null where they gave no rating.
 */
export const testimonials = [];
