{"console":{"identifier":"console","description":"The main window's application commands, allowed for the configured production origin and local development content. Keep remote.urls aligned with control_plane::DEFAULT_BASE_URL. Marketplace login windows receive no grant. notification:default is here rather than in default.json because on Android the main window is this remote origin, and the notification plugin's own page script asks is_permission_granted on every load; without the grant that call is a rejected promise in every page's log.","remote":{"urls":["https://teachouse.io"]},"local":true,"windows":["main"],"permissions":["allow-connect-marketplace","allow-session-status","allow-forget-session","allow-device-check-in","allow-device-activity","allow-start-import","allow-continue-import","allow-stop-import","allow-set-theme","allow-library-entries","allow-library-usage","allow-library-read","allow-library-remove","allow-library-settings","allow-set-library-settings","allow-library-open-external","notification:default"]},"default":{"identifier":"default","description":"Local grants for main, including allow-retry-console for the bundled offline page. Remote console commands are granted separately in console.json; updater permissions live in desktop-only updater.json. Tauri injects IPC into all webviews, so permissions provide the isolation. Desktop marketplace windows have no grants. Android marketplace pages reuse main, so console.json must restrict remote access to exactly DEFAULT_BASE_URL.","local":true,"windows":["main"],"permissions":["core:event:default","core:window:default","os:allow-hostname","allow-retry-console"]},"opener":{"identifier":"opener","description":"Handing one marketplace URL to the browser the seller already uses, and nothing else. Its own file rather than a line in default.json because it needs a remote block and that file deliberately carries none: adding one there would extend core:event, core:window, os:allow-hostname and allow-retry-console to the control plane's origin as a side effect of this change. The url must equal the one console.json names and control_plane::DEFAULT_BASE_URL, for the reason console.json states; web/src/lib/desktop.test.ts asserts the two capability files agree. Granted locally as well, because `local` defaults to true and the development override serves the console from the vite origin, where the window is local content. No platforms list, unlike updater.json: the plugin declares an Android implementation and Android is the platform this is most needed on, where the webview has no second window and a marketplace link replaces the console itself. allow-open-url rather than opener:default, which also carries open-path and reveal-item-in-dir — a filesystem surface, and one this grants to a remote origin. The scope is not decoration on that permission but the half that makes it work: allow-open-url arrives with an empty allow list, and tauri-plugin-opener 2.5.5 answers is_url_allowed with `self.allowed.iter().any(..)` (src/scope.rs), so the bare permission refuses every url. https only, which is narrower than the plugin's own allow-default-urls set: it leaves out mailto:, tel: and plaintext http:, and every link this console opens — a marketplace listing url and a catalogue homepage — is https. The founder authorised `opener:allow-open-url`; the scope narrower than upstream's default was measured and accepted on 2026-09-06, so widening it back to opener:default or to allow-default-urls is a new decision rather than a fix. The window is main, so the marketplace login webviews keep having no grant to anything, which is the fence default.json describes.","remote":{"urls":["https://teachouse.io"]},"local":true,"windows":["main"],"permissions":[{"identifier":"opener:allow-open-url","allow":[{"url":"https://*"}]}]},"updater":{"identifier":"updater","description":"The updater's grant, on the platforms that have an updater. Split out of default.json rather than left beside the other window grants, because tauri-plugin-updater is a desktop-only dependency: it declares platforms.support.android.level = \"none\" in its own manifest, lib.rs registers it under #[cfg(desktop)], and its Cargo entry is scoped to cfg(not(any(target_os = \"android\", target_os = \"ios\"))). A capability that names a permission whose plugin is not a dependency of the target being built is a build failure and not a silent no-op — the Android build stops with `Permission updater:default not found` — so the platform list below is what makes the split work rather than decoration on it. The window is main because that is the only window this application creates and the only one the updater's own commands are reached from; the login webview is absent from every capability, here as everywhere.","local":true,"windows":["main"],"permissions":["updater:default"],"platforms":["linux","macOS","windows"]}}