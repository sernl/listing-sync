{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The console window, and only it. The login webview is absent from every capability on purpose: a marketplace page must have no IPC surface at all, because dynamic per-webview scoping is unreliable and the site's own CSP would block ipc.localhost anyway.",
  "windows": ["main"],
  "permissions": [
    "core:event:default",
    "core:window:default",
    "os:allow-hostname",
    "updater:default"
  ]
}
