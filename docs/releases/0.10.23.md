# Teachouse 0.10.23

Fix: the Tes transport admits the image stage host (`upload.uploadcare.com`) on the anonymous route, so the cover hop added in 0.10.19 leaves the device. Native; the gateway transport shares the rule. No migration.
