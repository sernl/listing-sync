# Teachouse 0.10.12

Fix: a marketplace session rotation stored by the check-in probe now rebuilds the device transport client. The cache was keyed on capture instant and cookie count, both unchanged by a rotation, so requests kept presenting the superseded cookies and Tes refused them as a lapsed session within minutes of a proven one. Native only; no server change, no migration.
