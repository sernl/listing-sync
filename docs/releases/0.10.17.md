# Teachouse 0.10.17

Fix: the Tes clients bind IPv4. Tes and its S3 bucket publish AAAA records, and a device whose Wi-Fi hands out no global IPv6 address spent a fixed six seconds failing the v6 connect before giving up, which the device trace reported as `connect` and the run as a not-sent retry. Also pins the 0.10.16 description rendering test to its paragraph-then-list output. Native and server; no migration.
