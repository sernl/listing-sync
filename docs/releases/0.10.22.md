# Teachouse 0.10.22

Fix: the payload route reads the lease's fetchable files through the ledger, under the tenant pin. Its raw-pool read answered nothing under forced row-level security, so the route refused every server-held byte it was ever asked for; only the cover ever needed one, since bundles come from the marketplace or the device library. Server only; no migration.
