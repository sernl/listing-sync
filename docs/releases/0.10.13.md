# Teachouse 0.10.13

Fix: the presigned S3 upload ceiling is now sized by the bundle (60 s + 1 s per 64 KiB, capped at 480 s) instead of a flat 150 s. The 2026-09-18 trial bundles are 9-28 MB and the tablet uplink measured 57-148 KB/s, so the flat ceiling cut roughly every second upload off mid-body and each create fell to a ten-minute reconcile. Server images change too (the gateway transport shares the path); no migration.
