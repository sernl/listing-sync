# Teachouse 0.10.19

Feature: a Tes create sets the cover image. The product cover is projected (`ListingProjection.cover`), described in the work order beside the files so the device fetches it, and the Tes adapter uploads it the way the uploader page does (observed 2026-09-19): staged at Uploadcare, imported by `upload-cover-image`, and set on the draft as `customThumbnails`. The publish reads the draft and restates its thumbnails rather than posting an empty list. Native and server; no migration. TPT carries the cover in its field set and does not post it yet.
