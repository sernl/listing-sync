# Teachouse 0.13.0

Every remaining console section gets the guided treatment 0.12 gave Automations: Catalogue (Resources, a resource's page, New resource, Your machines' files, Labels, Collections, Analytics, Templates, Export), Import (Import, an import run, a spreadsheet batch, requests), Marketplaces (Marketplaces, Marketplace status), Help and guides, and the Admin back office. Numbered steps with one sentence each, diagrams of what goes where (files → thumbnail → marketplaces, collection → shops, source → catalogue with the counts on the arrow), colour-coded names, prices and marks, compact tables, filter chips, the primary action beside the step on desktop and in a sticky bar on phones, and every long explanation behind "Why?" or "How it works". Sticky footers now actually stick (the content region clips rather than hides overflow). The two dismissable banners are gone with their storage key; their text sits behind Explain.
