# Teachouse 0.10.21

Fix: the payload route lists a lease's fetchable files from the ledger rows (the leased product's live payloads and cover) instead of re-preparing the item, which refused an item whose write attempt was already open. That is when a run fetches bytes it has not cached: the cover, on a device that read the bundle from its own library. Server only; no migration.
