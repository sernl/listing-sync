# Teachouse 0.10.15

Feature: `POST /v1/jobs` accepts `intent: "remove"` and lowers each bound mapping to the removal of the listing it names, in the state the bind recorded (draft or live). Unbound, in-flight and lifecycle-unknown mappings are refused at submit. The engine, the driver absence poll and both adapters already executed removals; only the migrate source-leg could mint one before. Server only; no migration.
